# ── Required ──────────────────────────────────────────────────────────────────

# Local base directory. All subdirectories become target folder buttons in the UI.
MUSIK_DIR=/musik

# Path to the yt-dlp binary
YTDLP_BIN=/home/youruser/yt-dlp_linux


# ── hifi-api (Tidal) ──────────────────────────────────────────────────────────
# Comma-separated list of hifi-api instance URLs.
# Defaults to built-in pool of 8 public servers if not set.
# For reliable lossless downloads, run your own instance (see README).
HIFI_API_URL=HIFI_API_URL=https://hifi.geeked.wtf,https://frankfurt-1.monochrome.tf,https://api.monochrome.tf,https://wolf.qqdl.site,http://localhost:8000,


# ── History ───────────────────────────────────────────────────────────────────
# Where to store the download history JSON file.
# Defaults to one level above MUSIK_DIR.
# HISTORY_FILE=/ssd-daten/download_history.json


# ── Nextcloud Sync (optional) ─────────────────────────────────────────────────
# Set to true to sync after every completed download.

NEXTCLOUD_ENABLED=false
NEXTCLOUD_USER=your_username
NEXTCLOUD_PASSWORD=your_app_password
NEXTCLOUD_URL=https://your-nextcloud.example.com/nextcloud
NEXTCLOUD_MUSIK_PATH=/Musik
